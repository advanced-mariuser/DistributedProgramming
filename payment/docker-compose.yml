version: '3.3'

services:
  payment:
    image: payment
    container_name: payment
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081" # GRPC API port
    environment:
      ORDER_DB_HOST: payment-db
      ORDER_DB_PORT: 3306
      ORDER_DB_NAME: payment
      ORDER_DB_USER: payment
      ORDER_DB_PASSWORD: ${DB_PASSWORD}
      ORDER_DB_MAX_CONN: 5
    depends_on:
      - payment-db
    restart: unless-stopped

  payment-db:
    image: percona:8.0
    container_name: paymentservice-db
    environment:
      MYSQL_DATABASE: paymentservice
      MYSQL_USER: paymentservice
      MYSQL_PASSWORD: ${DB_PASSWORD}
      MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
    ports:
      - "3306:3306"
    volumes:
      - payment-db-data:/var/lib/mysql
    restart: unless-stopped

volumes:
  payment-db-data:
